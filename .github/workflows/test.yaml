name: Test KubeVirt Chart builder
# WIP
on:
  push:
    branches:
      - master
      - main

jobs:
  test:
    name: Test KubeVirt Chart builder

    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.24'
      - name: Run tests
        run: |
          go get .
          go test ./...
